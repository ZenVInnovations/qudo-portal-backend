apiVersion: v1
kind: Service
metadata:
  name: qudo-portal-frontend
  namespace: qudo-portal-dev
  labels:
    app.kubernetes.io/name: qudo-portal-frontend
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: qudo-portal-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
